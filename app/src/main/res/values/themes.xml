<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tippy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/surface1</item>
        <item name="colorPrimary">@color/base</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:colorBackground">@color/base</item>
    </style>

    <style name="Theme.Tippy" parent="Base.Theme.Tippy" />
</resources>